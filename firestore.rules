
/**
 * @file Overview
 * This ruleset enforces a strict user-ownership model for most data, with public read access for rewards.
 *
 * Data Structure:
 * - /users/{userId}: Stores user profile information. Users can only read/write their own profile.
 * - /users/{userId}/recycling_records/{recordId}: Stores recycling records. Only the owner can read/write.
 * - /users/{userId}/redemptions/{redemptionId}: Stores redemption records. Only the owner can read/write.
 * - /rewards/{rewardId}: Stores reward information. Publicly readable, admin-only write access.
 * - /roles_admin/{userId}: Indicates admin privileges.
 *
 * Key Security Decisions:
 * - User data is private. Listing other users is not allowed.
 * - The /rewards collection is publicly readable.
 * - Admin privileges are managed via the /roles_admin collection.
 *
 * Denormalization for Authorization:
 * - User-specific data is nested under /users/{userId} to simplify ownership rules.
 */
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    /**
     * @description Checks if the current user is signed in.
     */
    function isSignedIn() {
      return request.auth != null;
    }

    /**
     * @description Checks if the user ID matches the authenticated user's ID.
     */
    function isOwner(userId) {
      return isSignedIn() && request.auth.uid == userId;
    }

    /**
     * @description Checks if the user is an existing owner of a document.
     */
    function isExistingOwner(userId) {
        return isOwner(userId) && resource != null;
    }
    
    /**
     * @description Checks if the user is an administrator.
     */
    function isAdmin() {
      return exists(/databases/(default)/documents/roles_admin/$(request.auth.uid));
    }

    /**
     * @description Enforces user-level access control for profiles.
     * @path /users/{userId}
     * @allow (get) User can read their own profile.
     * @allow (create) User can create their own profile.
     * @allow (update) User can update their own profile.
     * @deny Reading, listing, or writing profiles of other users.
     */
    match /users/{userId} {
      allow get: if isOwner(userId);
      allow list: if false; // Disallow listing for leaderboard
      allow create: if isOwner(userId) && request.resource.data.id == userId;
      allow update: if isExistingOwner(userId) && request.resource.data.id == resource.data.id;
      allow delete: if isExistingOwner(userId);
    }

    /**
     * @description Enforces user-level access control for recycling records.
     * @path /users/{userId}/recycling_records/{recordId}
     * @allow (get, list, create, update, delete) Only the owner can manage their records.
     */
    match /users/{userId}/recycling_records/{recordId} {
      allow read, write: if isOwner(userId);
    }

    /**
     * @description Allows public read access to rewards, but restricts writes to admins.
     * @path /rewards/{rewardId}
     * @allow (get, list) Any user can read rewards.
     * @allow (create, update, delete) Only admins can manage rewards.
     */
    match /rewards/{rewardId} {
      allow get, list: if true;
      allow create, update, delete: if isAdmin();
    }

    /**
     * @description Enforces user-level access control for redemptions.
     * @path /users/{userId}/redemptions/{redemptionId}
     * @allow (get, list, create, update, delete) Only the owner can manage their redemptions.
     */
    match /users/{userId}/redemptions/{redemptionId} {
      allow get, list, create, update, delete: if isOwner(userId);
    }

    /**
     * @description Controls access to admin role documents.
     * @path /roles_admin/{userId}
     * @allow (get, create, delete) Only admins or the user themselves can manage admin roles (within limits).
     * @deny (list, update) Listing or updating roles is restricted.
     */
    match /roles_admin/{userId} {
        allow get: if isAdmin() || isOwner(userId);
        allow list: if false;
        allow create: if isAdmin();
        allow update: if false;
        allow delete: if isAdmin();
    }
    
    /**
     * @description  Denies all access to the recycling_records_all collection.  There is no such collection in the data model.
     * @path /recycling_records_all
     */
    match /recycling_records_all/{doc=**} {
      allow read, write: if false;
    }
  }
}
