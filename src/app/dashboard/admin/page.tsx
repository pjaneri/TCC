"use client";

import {
  Card,
  CardDescription,
  CardHeader,
  CardTitle,
} from "@/components/ui/card";

export default function AdminPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Funcionalidade Desativada</CardTitle>
        <CardDescription>
          A seção de administração está temporariamente desativada.
        </CardDescription>
      </CardHeader>
    </Card>
  );
}
